# WebSocket server URL for the frontend
# Use ws://localhost:3006 if running browser on the same machine as the server
# Use ws://192.168.254.2:3006 if accessing from a different machine on the network
# Or use ws://YOUR_SERVER_IP:3006 for remote access
NEXT_PUBLIC_UI_WS_URL=ws://localhost:3006
